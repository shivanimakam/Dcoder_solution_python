n,d=list(map(int,input().split()))
l=list(map(int,input().split()))
d=d%n
for i in range(d):
  last=l[0]
  for j in range(1,len(l)):
    l[j-1]=l[j]
  l[n-1]=last
for k in range(len(l)):
  print(l[k],end=" ")
    
